<nav class="navbar navbar-default navbar-fixed-top">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1 col-md-10 col-md-offset-1">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to root_path, class: "navbar-brand" do %>
          <strong>swarm</strong>
        <% end %>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class="nav-item <%= 'current' if current_page?(questions_path) %>">
            <%= link_to "Fragen", questions_path %>
          </li>
        </ul>
        <%= form_tag(search_path,
                     method: "get",
                     class:  "navbar-form navbar-left",
                     enforce_utf8: false) do %>
          <div class="form-group">
            <%= text_field_tag :q, nil, class: 'form-control', placeholder: 'Suchen...'%>
            <%# submit_tag "Search", name: nil %>
          </div>
        <% end %>
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <li class="nav-item <%= 'current' if current_page?(new_question_path) %>">
              <%= link_to "Neue Frage", new_question_path %>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <%= current_user.username %>
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profil bearbeiten", edit_user_registration_path  %></li>
                <li><%= link_to "Ausloggen", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          <% else %>
            <li class="nav-item <%= 'current' if current_page?(new_user_session_path) %>">
              <%= link_to "Login", new_user_session_path %>
            </li>
            <li class="nav-item <%= 'current' if current_page?(new_user_registration_path) %>">
              <%= link_to "Registrieren", new_user_registration_path %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
